version: 3

projects:
- name: sandbox-demo
  dir: deployments/sandbox-demo/us-east-1/sandbox-demo/infra
  autoplan:
    # enabled: true
    when_modified: ["**/*.hcl", "**/*.tf*"]

# - name: sandbox-deom-2
#   dir: deployments/sandbox-deom-2/*
#   autoplan:
#     enabled: true
    # when_modified: ["**/*.hcl", "**/*.tf*"]

# - name: central-iam-roles
#   dir: central/basic/iam-roles
#   autoplan:
#     when_modified: ["**/*.hcl", "**/*.tf*"]
#   workflow: central

# - name: central-iam-users
#   dir: central/basic/iam-users
#   autoplan:
#     when_modified: ["**/*.hcl", "**/*.tf*"]
#   workflow: central

# - name: development-iam-roles
#   dir: development/basic/iam-roles
#   autoplan:
#     when_modified: ["**/*.hcl", "**/*.tf*"]
#   workflow: development

# - name: staging-iam-roles
#   dir: staging/basic/iam-roles
#   autoplan:
#     when_modified: ["**/*.hcl", "**/*.tf*"]
#   workflow: staging

# - name: production-iam-roles
#   dir: production/basic/iam-roles
#   autoplan:
#     when_modified: ["**/*.hcl", "**/*.tf*"]
#   workflow: production
